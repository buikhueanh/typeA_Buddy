# Copy to .env and fill real values.
# Never commit .env

# --- Server (services/api) ---
API_PORT=8000
API_ENV=development

# If you add a DB later:
# DATABASE_URL=postgresql://user:password@localhost:5432/aiplanner

# --- LLM provider (later) ---
LLM_PROVIDER=gemini
LLM_API_KEY=YOUR_KEY_HERE
EOF